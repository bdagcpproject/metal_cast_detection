runtime: python
env: flex

runtime_config:
  operating_system: "ubuntu22"
  runtime_version: "3.11"

entrypoint: streamlit run main.py --server.port $PORT

resources:
  disk_size_gb: 20

automatic_scaling:
  min_num_instances: 1
  max_num_instances: 2